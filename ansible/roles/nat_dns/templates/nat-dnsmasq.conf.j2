# Auto-generated by Ansible (nat_dns role) – do not edit manually.

# Listen only on the NAT VLAN VIP so we don't clash with systemd-resolved.
interface={{ vlan_if }}
listen-address={{ nat_dns_listen_ip }}
bind-interfaces

# Don't use /etc/resolv.conf; we specify upstreams ourselves.
no-resolv

# Be strict: only forward valid domain queries
domain-needed
bogus-priv

{% for s in nat_dns_upstream_servers %}
server={{ s }}
{% endfor %}
